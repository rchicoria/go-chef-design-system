import React, { Component } from 'react';

import colors from '../colors.js';
import Icon from './Icon';

export const SearchNavBarIcon = (props) => (
  <Icon name="SearchSelected" width="30" height="30" />
)

export const SearchNavBarIconActive = (props) => (
    <Icon name="Search" width="30" height="30" />
  )
