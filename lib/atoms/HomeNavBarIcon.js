import React, { Component } from 'react';

import colors from '../colors.js';
import Icon from './Icon';

export const HomeNavBarIcon = (props) => (
  <Icon name="FeedSelected" width="30" height="30" />
)

export const HomeNavBarIconActive = (props) => (
    <Icon name="Feed" width="30" height="30" />
  )
