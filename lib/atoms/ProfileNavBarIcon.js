import React, { Component } from 'react';

import colors from '../colors.js';
import Icon from './Icon';

export const ProfileNavBarIcon = (props) => (
  <Icon name="ProfileSelected" width="30" height="30" />
)

export const ProfileNavBarIconActive = (props) => (
    <Icon name="Profile" width="30" height="30" />
  )
